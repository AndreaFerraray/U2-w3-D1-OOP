class User {
  constructor(firstName, lastName, age, location) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.age = age;
    this.location = location;
  }
  static isOlder(Users) {
    return User.age > User.age;
  }
}
const Users = [
  new User("Gino", "Pinguino", "32", "Roma"),
  // console.log(ginoPinguino);
  new User("Marco", "Pilo", "18", "Milano"),
  new User("fabrizio", "Turo", "28", "Roma"),
  new User("Giulia", "Cafro", "32", "Napoli"),
];
console.log(User.isOlder());

// ----------------esercizio 2-----------------------

class Pet {
  constructor(firstName, lastName, age, location) {
    this.petName = petName;
    this.ownerName = ownerName;
    this.species = species;
    this.breed = breed;
  }
  static ownerName(Pet) {
    if { pet.ownerName === Pet.ownerName;
        return true }
  }
}
const pets=[
    new Pet('ciccio', 'graziano','cane', 'belga')
    new Pet('dari', 'graziano', 'cane', 'alano',)
    nwe Pet('frew','Tiziana' ,'coniglio', 'nn')
]
